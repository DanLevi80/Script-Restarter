Config = {}

-- List of scripts to restart and their interval in minutes
Config.ScriptsToRestart = {
    { name = "your_script1", interval = 10 }, -- Restart every 10 minutes
    { name = "your_script2", interval = 15 }, -- Restart every 15 minutes
    { name = "your_script3", interval = 30 }, -- Restart every 30 minutes
}

-- Debug mode (true = show messages in console)
Config.Debug = true
